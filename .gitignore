# Byte-compiled
__pycache__/
*.py[cod]

# Everything this tree generates. `paths.py` routes outputs by type; none of it
# belongs in the repo.
output/

# Environments / caches
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Agent workspace files. CLAUDE.md itself is tracked here on purpose:
# it is this project's own orientation doc, not a local scratch file.
.claude/

# Logs
*.log

# Local dev config: pins this machine's conda prefix.
pyrightconfig.json
